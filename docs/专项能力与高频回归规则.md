# 专项能力与高频回归规则

本索引记录需要跨模块长期保持的专项实现边界。新增或修复具有复用价值的问题时，应更新对应分卷，并在代码、数据库、接口、界面与验收之间保持一致。

| 专项能力                   | 规则分卷                                                         | 适用范围                                       |
| -------------------------- | ---------------------------------------------------------------- | ---------------------------------------------- |
| Webhook 可靠投递与密钥治理 | [Webhook 可靠投递与密钥规则](rules/Webhook可靠投递与密钥规则.md) | 应用配置、异步终态、Svix、签名密钥、重试和死信 |
