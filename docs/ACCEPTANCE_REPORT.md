# VeriScan 本地交付验收报告

日期：2026-09-01

范围：当前仓库、本机开发环境与隔离 Docker 容器；不代表生产环境上线验收。

## 1. 结论

当前版本已经形成可运行的生产候选基线：应用与独立 API Key、同步批量审核、不可变规则版本、三种外部 AI 协议、管理后台、审核记录与按应用/Key 统计、混合缓存、OpenTelemetry、迁移任务、liveness/readiness 和非 root 容器镜像均有代码与自动化证据。

但当前结论不是“可直接无条件上线”。真实供应商、生产 OIDC/TLS、备份恢复、故障演练和整链路容量仍需在目标环境验收；技术方案里的整体 QPS、AI 延迟和准确率仍是目标值，不能用本报告中的进程内规则基线替代。

## 2. 已验证证据

| 范围       | 本地证据                                                                                         | 结论边界                                                                                |
| ---------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- |
| 后端       | `dotnet test VeriScan.slnx`、Release build、format                                               | 覆盖 API 契约、鉴权、幂等、AI fixture、规则、统计、缓存、观测与健康探针；不含真实供应商 |
| 前端       | 33 个 Vitest 用例，lint、typecheck、Prettier、Vite production build                              | 覆盖数据适配和关键表单逻辑；视觉回归仍需持续自动化                                      |
| 数据库     | PostgreSQL 16 上验证全新迁移和包含旧应用/词条/审核记录的升级回填                                 | 证明迁移路径可执行，不等于生产备份恢复演练                                              |
| 本地依赖   | PostgreSQL、Redis、Keycloak 隔离启动为 healthy，OIDC discovery 返回 200                          | 仅本地 Compose，不证明生产高可用                                                        |
| API 镜像   | ARM64 实际构建，约 57.0 MB，UID 1654；`/healthz` 200，断库时 `/readyz` 503，迁移后 `/readyz` 200 | 证明探针语义、迁移入口和非 root 运行，不证明目标集群网络与密钥配置                      |
| 管理端镜像 | ARM64 实际构建，约 25.6 MB，UID 101；`/` 与 `/rules` 均返回 SPA                                  | OIDC 参数在镜像构建时注入，生产发布需使用环境对应值                                     |
| 管理端视觉 | Chromium 桌面 1440×1100、移动端 390×844 检查                                                     | 已统一深青色主按钮与白色文字；不代表全浏览器矩阵                                        |

## 3. 规则引擎本机基线

命令：

```bash
dotnet run --project tests/performance/RuleEngine.Baseline/RuleEngine.Baseline.csproj \
  --configuration Release -- 10000 20000
```

环境：Apple M1 Max、10 个可见处理器、64 GiB 内存、Darwin arm64、.NET 10.0.11。输入为 10,000 条词规则、32 字符文本、20,000 次热运行。

| 指标         |          结果 |
| ------------ | ------------: |
| 编译规则版本 |     28.328 ms |
| 单次均值     |      3.384 μs |
| P95          |      3.542 μs |
| P99          |      5.291 μs |
| 进程内吞吐   | 295,486 ops/s |
| 单次分配     |   1,432 bytes |

该结果只衡量已经编译并按不可变版本缓存的多模式词匹配与判定，不含 HTTP、PostgreSQL、API Key、JSON、日志、外部 AI 或网络。它不能证明“整链路 QPS 1500+”，也不能与二级 AI 延迟相加后直接推导生产容量。

## 4. 上线前必须补充的目标环境门禁

- 使用目标 OIDC、TLS、域名、密钥系统和网络策略完成认证与 SSRF 边界验收；
- 对每个实际 AI Provider 执行合成请求，核对 schema、超时、429/5xx、费用与数据出境要求；
- 用匿名化代表性数据执行完整 HTTP 压测，分别测规则直拒、AI 路由和 Provider 故障三种曲线；
- 演练 PostgreSQL 备份恢复、迁移回滚策略、Redis 故障、Provider 超时风暴和 Key 泄露撤销；
- 建立独立标注评测集后再发布准确率、召回率和阈值，不能沿用方案估算；
- 对容器基础镜像使用发布时固定 digest，并执行镜像、依赖、许可证和 SBOM 扫描。

## 5. 明确保留的后续能力

当前版本没有把组合规则、正则沙箱、RAG、多模态、异步批次/Webhook、双人审批、灰度比例、主动学习和多租户计费伪装成已完成。`review` 仍是 VeriScan 的机器审核终态，只返回调用方并记录事实，不建设平台人工复审队列。
