## 0. 总则

- 动手前先确认任务目标与用户要求一致。
- 优先使用与任务匹配的技能和工具。
- 所有通用能力与基础设施必须先检索、评估并优先采用成熟开源方案；认证、授权、IAM、OAuth/OIDC、MFA、密码、Token、Session、任务调度、消息、缓存、文件、可观测性等已有成熟方案覆盖的领域，禁止重复自研协议、密码学原语、状态机或数据库运行时。只有在许可证、安全、部署、兼容性或业务硬约束经可审计证据证明现有方案均不满足，并获得用户明确批准后，才允许自研最小缺口；“更可控”“更原子”“更完整”不能单独作为自研理由。
- 发现既有自研能力已被成熟开源方案覆盖时，优先通过可恢复的 Git revert 或独立清理提交移除旧实现，保留必要的数据迁移与业务适配边界，禁止为了保住既有投入继续扩展错误方向。
- 项目尚未上线，允许破坏性更新，但必须保持结构稳定。
- 修复具有复用价值的问题后，必须同步更新对应规则文档：通用基线更新本文件，专项能力与高频回归细则按 `docs/专项能力与高频回归规则.md` 索引更新对应分卷，避免同类问题反复出现。
- 所有注释必须使用中文。

## 1. AI 工作方式与完成标准

- 先识别任务所属模块，再读已有实体、DTO、接口、页面后决定改法。
- 优先复用既有模式，避免生成第二套风格。
- 新增字段、索引、权限码、路由名保持命名一致。
- 修改权限、菜单、登录、JWT、字典、参数、关键写操作时，必须同步检查相关前后端、缓存与审计链路。
- 业务规则中的数量、阈值、时长、比例、上下限、提醒偏移等数字，默认不得硬编码；优先落到后台可配置的系统参数或既有业务配置实体，并补参数目录与测试。
- 命名规范：后端实体/DTO 使用 PascalCase；接口/服务使用明确业务前缀；React 组件使用 PascalCase；Hook 使用 `useXxx`；API 方法使用“动词 + 领域名”。
- 模块完成标准：数据模型落地、接口可调用、权限接入、前端可用、基本校验完成、关键写操作有审计、必要测试补齐或明确缺口。
- 明确禁止：直接返回数据库实体、在 Handler 堆业务逻辑、前端写死未定义权限码、只做前端隐藏不做后端鉴权、硬编码敏感配置、明文写日志、无约束复制页面。
- 默认实现顺序：基础设施与认证 -> 用户角色 -> 菜单权限 -> 登录与动态路由 -> 用户/角色/菜单页面 -> 字典 -> 参数 -> 调度 -> 文件 -> 日志审计。
- 提交说明至少交代：涉及模块、是否引入数据库迁移、是否新增权限码、是否需补种子、是否需前端同步菜单/路由、未覆盖的测试风险。
- 禁止在前端界面显示任何技术性提示，不该给用户看见的技术性或解释性内容禁止出现。
